<p>
</p>
  <%=h @category.name %>
<p>
<b>Cartoons in this category:</b><br />
  <% @category.cartoons.each do |cartoon| %>
    <%= link_to image_tag(cartoon.artwork.url(:thumb)), cartoon_path(cartoon) %>
  <% end %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
